// @flow

import type { Node } from "react";

import { Fragment } from "react";

type Props = {
  urgency: number,
  importance: number,
  title: string,
};

export default function Dot(props: Props): Node {
  return <Fragment />;
}
